const store = () => {
    let state = {};

    const getState = () => {
        return state;
    }

    const setState = (action) => {
    }

    return { getState, setState }
}

export default store;